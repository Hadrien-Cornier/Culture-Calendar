# v12f — Single-Row Rect AND

```
variant: v12f
row_count:          1row
chip_shape:         rect
multi_select:       multi-and
stickiness:         sticky-top
icon_text:          text-only
breakpoints:        mobile-first-wrap
state_persistence:  url-only
notes:              Venue and category on one wrap row separated by a bullet; rectangular chips; multi-select within each, AND across
```
