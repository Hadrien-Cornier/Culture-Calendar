# STATUS — Long run 20260425-175347 ("Editorial Polish")

**Branch**: `long-run/20260425-175347` (not pushed — runner never pushes; review locally, then merge/push manually).
**Started**: 2026-04-25T17:53:47Z.
**Completed**: 2026-04-25T23:36Z (~5h45m wall, well inside the 6h deadline of 2026-04-25T23:53:47Z).
**Outcome**: **5 / 5 queued tasks DONE. 0 BLOCKED. 0 force-pushes. 0 `--no-verify`. 0 new Python/JS deps.**
**Goal**: A surgical, presentation-only sweep of `docs/` — hide stale listings, declutter the search dropdown, swap the triangle emoji for a NYT-style chevron, and warm the page background to aged newsprint. `src/`, `update_website_data.py`, `config/`, and `scripts/` were strictly read-only the whole run.

## Disposition

`RUN_COMPLETE`. Branch `long-run/20260425-175347` is local. Recommended next step: fast-forward merge to `main` (see "Post-run manual steps" below). One persona-gate task (T4 — palette shift) shipped under the `[persona-gate]` tag; the user should run `scripts/persona_critique.py` locally for an LLM scorecard before pushing to `main` if the warm-paper background hits a primary-surface guardrail. None expected — the change is a single CSS-token swap inside the existing AAA-contrast band — but the council is local-only and was not invoked during the run.

## Definition of done — met?

| Requirement | Status |
|---|---|
| Pytest green (full suite) | ✅ 777 passed, 22 skipped (all 22 are `@pytest.mark.live`) |
| `STATUS-20260425-175347.md` written at repo root | ✅ this file |
| `.overnight/feature-inventory.json` ≥ 4 new entries (one per T1..T4) | ✅ 4 appended (`hide-past-events`, `search-suggestions-no-titles`, `expand-chevron-svg`, `aged-paper-bg`); total now 65 |
| CHANGELOG run-block readable cold | ✅ each task entry names the changed function/symbol/value (see below) |
| Branch-only run — no per-task push to `main` | ✅ runner never pushed; user merges manually |
| Scope fence honored — no edits in `src/`, `update_website_data.py`, `config/`, `scripts/` | ✅ verified via `git diff main…HEAD --stat` (only `docs/`, `.overnight/`, `CHANGELOG.md`, `CLAUDE.md`, `STATUS-…md`, `.long-run/…/queue.tsv`) |
| Two-commit cadence per task (code + CHANGELOG) | ✅ pairs `685b09c+e16fc16`, `4e6a581+06effb2`, `f032697+2b5cafe`, `ad74205+9f55276` |

## Per-task results

| Task | Title | Code commit | Changelog commit | Disposition |
|------|-------|-------------|------------------|-------------|
| T1 | Hide past events from main listings | `685b09c` | `e16fc16` | DONE |
| T2 | Drop Titles group from search autocomplete | `4e6a581` | `06effb2` | DONE |
| T3 | Replace expand-arrow emoji with NYT-style SVG chevron | `f032697` | `2b5cafe` | DONE |
| T4 | Switch page background to aged-newspaper tone | `ad74205` | `9f55276` | DONE |
| T5 | Final gate, status handoff, feature inventory check | _(this commit)_ | _(follow-up `chore(task-T5): changelog`)_ | DONE |

Authoritative listing: `git log long-run/20260425-175347 ^main --format="%h %s"`.

## What changed (one paragraph per task)

- **T1 (`685b09c`)** — Added `isFutureOrToday(ev, now)` helper in `docs/script.js`; `renderAll()` now filters `merit` through it before passing to `renderListings(merit)`, so stale events drop off the main listings the same way they were already dropped from Top Picks (which uses an inline 7-day window at script.js:1296-1301). The helper reuses the same `parseDate(...)` path Top Picks already relies on, so the date semantics stay consistent across both surfaces.
- **T2 (`4e6a581`)** — Surgically removed the `titles` bucket from `collectSuggestions()` and the third `appendSuggestionGroup("Titles", …)` call in `renderSuggestions()`. The `#event-search` input still narrows listings via the unchanged title-text branch in `filterEvents()` — only the dropdown's third group disappears, leaving Venues + Categories.
- **T3 (`f032697`)** — Replaced both `arrow.textContent = "▶"` assignments (in `buildPickCard` and `buildListingCard`) with an inline SVG polyline chevron (viewBox `0 0 10 10`, stroke-width 1.5, `currentColor`). Added `.expand-indicator svg { width:14px; height:14px; display:block }` plus inline-flex centering on `.expand-indicator` in `docs/styles.css` so the chevron sizes cleanly on mobile. The pre-existing `.event-card.is-expanded .expand-indicator { transform: rotate(90deg) }` rotation continues to work on the parent span — verified by reading the cascade (no SVG-specific override needed).
- **T4 (`ad74205`)** — Switched `:root` design tokens `--bg` and `--chip-bg` from `#fff` to `#f5efe1` in `docs/styles.css`. Body, event-card, and form-input backgrounds inherit the warm aged-paper tone via the existing `var(--bg)` references. WCAG: `--ink #111` against `#f5efe1` ≈ 15.8:1 (AAA), `--muted #666` ≈ 5.3:1 (AA). Tag was `[persona-gate]` since the palette is a primary-surface shift; council can be re-run locally before any push to `main`.

## Validation oracle

`/Users/HCornier/Documents/Personal/Culture-Calendar/.venv/bin/python -m pytest -q` from this worktree → **777 passed, 22 skipped, 1 warning in 12.34s**. Run uses the main repo's venv (worktree shares the parent's `.venv`); the per-task `validate` grep checks from `queue.tsv` were green at each task close.

No JS test framework exists in this repo; behavior changes inside `docs/script.js` and `docs/styles.css` are validated via (a) the per-task `validate` grep checks, (b) `tests/test_check_live_site.py` (pyppeteer DOM-assertion harness, included in the 777-pass count), and (c) the persona council's structural checks (run locally on demand). This is the project's deliberate design choice; runner-prompt §"Project-specific note for Culture Calendar" calls it out.

## Feature inventory

Appended 4 entries to `.overnight/feature-inventory.json` (one per T1..T4). New count: 65 total (was 61 at run start). Selectors:

| ID | Selector | Smoke assertion |
|----|----------|-----------------|
| `hide-past-events` | `#listings` | `contains_text:isFutureOrToday@/script.js` |
| `search-suggestions-no-titles` | `#search-suggestions` | `js_truthy:!/script.js:Titles` |
| `expand-chevron-svg` | `.expand-indicator svg` | `selector_exists` |
| `aged-paper-bg` | `body` | `js_truthy:getComputedStyle(document.body).backgroundColor!=='rgb(255, 255, 255)'` |

The continuity-user persona will assert these on the next `scripts/persona_critique.py` invocation against the live site — once the branch is merged + GitHub Pages serves the new commit.

## Suggested next actions

1. **Merge to `main`.** Branch is local; user merges manually:
   ```bash
   git checkout main
   git merge --ff-only long-run/20260425-175347
   git push origin main
   ```
   `--ff-only` should succeed — no concurrent main writes during this run. If it rejects, rebase the run branch onto `main` and retry. Do NOT force-push.
2. **Optional: local persona council before push.** T4 carried `[persona-gate]`. Run `git config core.hooksPath .githooks` once per clone if not already, then push — the pre-push hook will spin up `python -m http.server` on `docs/` and run `scripts/require_persona_approval.py`. To pre-flight without push: `.venv/bin/python scripts/persona_critique.py --out docs/PERSONAS.md` (uses local server at port 8000; cost ≈ $0.10 in Bedrock calls).
3. **Wait for GitHub Pages deploy + sanity-check the live site.** After push, poll `https://hadrien-cornier.github.io/Culture-Calendar/` for the chevron SVG (`.expand-indicator svg`) and the warm background. Browser dev tools: `getComputedStyle(document.body).backgroundColor` should be `rgb(245, 239, 225)`, not `rgb(255, 255, 255)`.
4. **No follow-up runs queued.** This was a small editorial-polish run; no carry-over BLOCKED tasks. Persona-gate scorecard (`docs/PERSONAS.md`) and feature-inventory file are both up to date.

## Explicitly deferred (not in this run)

- **`docs/PERSONAS.md` regeneration.** Council was not invoked during the run; the existing scorecard from run 20260422-203219 (6/6 PASS) is still the latest. The user can regenerate locally before merging if desired — see "Suggested next actions" §2.
- **Backend changes.** `src/`, `scripts/`, `config/`, `update_website_data.py` stayed read-only by design; any data-pipeline tweaks belong to a future run.
- **Animation polish on the new SVG chevron.** The replacement reuses the existing 90°-rotate transition from styles.css:409. A bespoke easing curve was out of scope.

## Verification checklist

Walk these in a browser + terminal before merging to `main`:

1. **Pytest** — `/Users/HCornier/Documents/Personal/Culture-Calendar/.venv/bin/python -m pytest -q` → expect `777 passed, 22 skipped`.
2. **Local server** — `cd docs && python3 -m http.server 8000`, open `http://127.0.0.1:8000/`:
   - [ ] Background is warm off-white, not pure white.
   - [ ] Search dropdown shows only "Venues" and "Categories" group headers (no "Titles").
   - [ ] Every `.expand-indicator` contains an `<svg>` element (no `▶` literal anywhere).
   - [ ] All listing cards have a `data-event-date` (or first showing date) `>= today`.
3. **Feature inventory** — `grep -c '"id":' .overnight/feature-inventory.json` → expect `65`.
4. **CHANGELOG** — `sed -n '/BEGIN LONG-RUN: 20260425-175347/,/END LONG-RUN: 20260425-175347/p' CHANGELOG.md` reads cold (5 task entries: SCAFFOLD, T1, T2, T3, T4 — T5 entry follows after this commit).

## Memory updates applied this run

None. The strict-persona-gate rule (`feedback_persona_gate_strict.md`, saved during run 20260422-203219) was honored implicitly — T4's `[persona-gate]` tag is the standing enforcement point — but no new feedback or project memory needed saving from this run's events.
