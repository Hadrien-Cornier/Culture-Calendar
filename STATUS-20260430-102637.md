# STATUS — Long run 20260430-102637 ("Sparkling Simon")

**Branch**: `long-run/20260430-102637` (local; runner never pushes — review here, then merge to `main` manually).
**Started**: 2026-04-30T15:32:24Z. **Deadline**: 2026-05-01T03:32:24Z. **Wall-clock at handoff**: ~4h elapsed, ~8h remaining.
**Outcome**: **33 / 38 tasks DONE. 2 terminally BLOCKED. 2 TODO (6.6b carry-over + 7.3 wrap-up). 0 force-pushes. 0 `--no-verify`. 0 new deps.**
**Goal**: Five overlapping workstreams — NYT-inspired typography, Ballet dance review handler, classical-data refresh automation, two new code-review LLM personas, and a karpathy/nanochat minimalism cleanup of root + `.overnight/` + `CLAUDE.md`/`CHANGELOG.md`.

## Disposition

`RUN_PARTIAL`. The five intended workstreams shipped fully or near-fully. Two cleanup tasks remain unresolved and need human judgment (see "Carry-over work" below) — they are the **classical-scraper consolidation** (Phase 6.6a/b) and one council-rejected `.long-run/` cleanup (6.5). All five primary deliverables — typography, ballet dance handler, classical refresh, code-review personas, minimalism cleanup of `.overnight/`/`CLAUDE.md`/`CHANGELOG.md` — are merged on the run branch.

## Definition of done — met?

| Requirement | Status |
|---|---|
| `update-calendar.yml` and `pr-validation.yml` pass on the run branch | ⏳ workflows not dispatched in this run; T1.x addressed the underlying breakage (verify_calendar tolerance, missing fixture) and pytest is green — flip to ✅ after a manual `gh workflow run`. |
| Ballet events tagged `type=dance`; `_get_dance_rating()` invoked; reviews mention choreographer/company/repertoire | ✅ T2.1 → T2.5. |
| `scripts/refresh_classical_data.py` with `--dry-run` + `.github/workflows/refresh-classical-data.yml` cron | ✅ T3.1a → T3.4. |
| `personas/live-site/*.json` × 6 + `personas/code-review/{review-quality,repo-minimalism}.json` × 2 | ✅ T4.1 → T4.6 (8 persona files present; pre-push hook + `scripts/review_quality_check.py` wired). |
| Source Serif 4 + Inter web fonts; CSS type-scale variables drive every typographic font-size in `docs/styles.css` | ✅ T5.1 → T5.6 (T5.6 carried `[persona-gate]`). |
| `.overnight/` deleted; root has no `*.log`; `CLAUDE.md` < 250 lines; `CHANGELOG.md` < 800 lines; `.long-run/` retains only the active run dir | ✅ all four (`.overnight/` gone, no `*.log` at root, `CLAUDE.md` = 249 lines, `CHANGELOG.md` = 387 lines, `.long-run/` holds only `20260430-102637/` + `active.pid`). |
| 5 classical scrapers consolidated into `_static_json_scraper.py` + thin per-venue config | ❌ **T6.6a BLOCKED** (no-result-file crash at 19:13:28Z) → **T6.6b TODO**. The five scrapers (`austin_symphony`, `early_music_austin`, `la_follia`, `austin_chamber_music`, `austin_opera`) are unchanged from the start of the run. |
| `pytest -q` exits 0; `verify_calendar.py --offline` passes; live-site persona council 6/6 PASS | ✅ pytest 856 passed / 25 skipped (verified at handoff); verify_calendar 22/22 + persona council 34 passed at T7.1. |
| ≥6 new feature-inventory entries (typography surfaces + new reviewers) | ✅ `config/feature-inventory.json` = 73 entries (typography surfaces appended in T5.x, reviewers + persona migration in T4.x). |
| `STATUS-20260430-102637.md` handoff written | ✅ this file. |

## Per-task results

Authoritative listing: `git log long-run/20260430-102637 ^main --format="%h %s"` (71 commits at handoff start, 73 after this task's two commits).

| Phase | Task | Title | Disposition | Notes |
|------:|------|-------|-------------|-------|
| 1 | 1.1 | Fix `_build_book_prompt` graceful fallback | DONE | `3ac999b` + `bf43fa8` |
| 1 | 1.2 | Scope title-rejection regex narrower | DONE | `627bd63` + `7b772c6` |
| 1 | 1.3 | Add missing Hyperreal fixture | DONE | `cdd536c` + `17db001` |
| 1 | 1.4 | `verify_calendar` Weekend tolerance | DONE | `f9a9074` + `757ad14` |
| 1 | 1.5 | Triage `claude-code-review` action | DONE `[persona-gate]` | `465c12b` + `cd930ec` |
| 2 | 2.1 | Ballet scraper `concert→dance` | DONE | `1510b62` + `e04bd75` |
| 2 | 2.2 | `_get_dance_rating` handler | DONE | `4cb1cdb` + `ce40778` |
| 2 | 2.3 | `_build_dance_prompt` summary prompt | DONE | `a4d537a` + `abe8bc8` |
| 2 | 2.4 | Ballet dance review e2e tests | DONE | `617f912` + `ae9b8bc` |
| 2 | 2.5 | Force-reprocess cached ballet entries | DONE | `14a4caa` + `e41f52a` |
| 3 | 3.1a | `refresh_classical_data.py` skeleton + validator | DONE | `6d62375` + `a0ea7a7` |
| 3 | 3.1b | Wire Perplexity LLM crawler | DONE | `f5ead36` + `6d2543d` |
| 3 | 3.2 | Monthly workflow `refresh-classical-data.yml` | DONE | `22409d0` + `8894f3d` |
| 3 | 3.3 | Run refresh script once locally | DONE | `a85c1c7` + `1bbadbe` |
| 3 | 3.4 | Document classical refresh pipeline | DONE | `150d183` + `6f6aa37` |
| 4 | 4.1 | Migrate personas + feature-inventory out of `.overnight/` | DONE | `4a6b304` + `8955bcc` |
| 4 | 4.2 | `review-quality.json` reviewer | DONE | `1037ab5` + `fd2b8d0` |
| 4 | 4.3 | `repo-minimalism.json` reviewer | DONE | `50b87dc` + `c5beb3c` |
| 4 | 4.4 | Wire `repo-minimalism` into pre-push | DONE | `c6665a5` + `3e9bc9f` |
| 4 | 4.5 | `review_quality_check.py` manual command | DONE | `49bf4bf` + `94e2568` |
| 4 | 4.6 | Document new reviewers in README + CLAUDE.md | DONE | `3f3e0c7` + `b1b3b6f` |
| 5 | 5.1 | Typography decision document | DONE | `02675a7` + `490c7ee` |
| 5 | 5.2 | Load NYT-inspired web fonts | DONE | `851bd76` + `c26bf42` |
| 5 | 5.3 | Define type-scale CSS variables | DONE | `80c6705` + `c3ae17b` |
| 5 | 5.4 | Apply scale to body + headings | DONE | `82e7849` + `7446f08` |
| 5 | 5.5 | Mobile typography pass with `clamp()` | DONE | `693f2d6` + `1cf9e3c` |
| 5 | 5.6 | Persona-gate typography commit + feature inventory | DONE `[persona-gate]` | `5920ed4` + `c2e0aab` |
| 6 | 6.1 | Root cleanup: logs + STATUS files | DONE | `23fdf56` + `7646abb` |
| 6 | 6.2 | Trim `CLAUDE.md` < 250 lines | DONE | `c57cd3e` + `7e23df4` |
| 6 | 6.3 | Trim `CHANGELOG.md` < 800 lines | DONE | `30fc1fb` + `78297aa` |
| 6 | 6.4 | Remove `.overnight/` directory | DONE `[persona-gate]` | `cbe9998` + `630c66b` |
| 6 | 6.5 | Cleanup historical `.long-run/` dirs | **BLOCKED** | First commit `3990262` deleted three old session dirs; rebuttal commit `7c61087` added `.gitignore` patterns. Council still flagged commit subject as misleading on round 2/2 — terminally rejected. The deletions DID land on disk; only the commit-message framing was rejected. |
| 6 | 6.6a | `_static_json_scraper.py` base class | **BLOCKED** | `no-result-file (rc=1)` at 19:13:28Z — agent crashed without writing the result file. No commit landed. Five classical scrapers unchanged. |
| 6 | 6.6b | Migrate 5 venues to base class | TODO | Depends on 6.6a; never started. |
| 6 | 6.7 | Audit + remove unused scripts | DONE | `335db0b` + `d4afdec` (7 scripts deleted) |
| 7 | 7.1 | Final gate full validation suite | DONE | `fc4d0ab` + `901e0b2` (pytest 856/25, verify_calendar 22/22, persona council 34, refresh dry-run ok) |
| 7 | 7.2 | Run handoff status document | DONE | _(this commit + follow-up CHANGELOG commit)_ |
| 7 | 7.3 | Run wrap-up | TODO | Final task; runner closes the queue after this. |

## Summary of what changed (one paragraph per workstream)

- **Phase 1 — workflow / data hygiene.** `_build_book_prompt` no longer raises when book metadata is partial (`src/summary_generator.py`); the title-rejection regex was scoped narrower so festival/workshop/gala/tribute titles flow through when other metadata is present; the missing Hyperreal fixture for `event_xxx_return_xander_cage_2026.html` was added; `scripts/verify_calendar.py` now tolerates a zero-event Weekend window so offline verification doesn't false-fail. `claude-code-review.yml` workflow was kept after a `[persona-gate]` triage decision documented in CHANGELOG.
- **Phase 2 — Ballet dance handler.** Root cause was `ballet_austin_scraper.py:66` hardcoding `type=concert`; now `type=dance`. New `_get_dance_rating()` in `processor.py` issues a dance-specific Perplexity prompt that asks about choreographer/company/repertoire; `summary_generator._build_dance_prompt` reads `program`/`series` fields. End-to-end test suite at `tests/test_ballet_dance_review.py`. Cached ballet entries force-reprocessed; `docs/data.json` now reflects the dance category.
- **Phase 3 — classical refresh automation.** New `scripts/refresh_classical_data.py` (dry-run-capable, schema-validating, Perplexity-driven crawler with stub fallback) plus `.github/workflows/refresh-classical-data.yml` cron `0 12 1 * *` that opens a PR `chore: monthly classical/ballet data refresh` on `bot/classical-refresh-<date>` — **intentionally never auto-merged**. `CLAUDE.md` documents the full pipeline (validator, failure mode, secrets).
- **Phase 4 — code-review personas.** `personas/live-site/*` × 6 migrated out of `.overnight/`; `personas/code-review/review-quality.json` (senior-engineer lens — flags diffs that weaken AI review generation) and `personas/code-review/repo-minimalism.json` (Karpathy/nanochat lens — flags ceremony, helper graveyards, premature abstraction) authored. `scripts/review_quality_check.py` manual command + `.githooks/pre-push` parse-validation of `repo-minimalism.json` on push.
- **Phase 5 — NYT typography.** Source Serif 4 (display + body serif) + Inter (UI sans) loaded via Google Fonts in `docs/index.html`; type-scale CSS variables (`--fs-display`, `--fs-h1`…`--fs-meta`, `--lh-tight`/`--lh-body`/`--lh-loose`) defined and applied to body + every heading + listing copy in `docs/styles.css`; mobile pass uses `clamp()` so the scale fluidly compresses below 600px. T5.6 carried `[persona-gate]`; the council-gated commit landed cleanly with feature-inventory entries appended.
- **Phase 6 — minimalism cleanup.** Root has zero `*.log` files (10 historical `STATUS-*.md` moved to `archive/runs/`); `CLAUDE.md` trimmed from a multi-thousand-line file to 249 lines (history at `archive/CLAUDE-runs.md`, 153 lines); `CHANGELOG.md` trimmed to 387 lines (older entries at `archive/CHANGELOG-history.md`, 1,266 lines); `.overnight/` directory deleted entirely (its contents had already moved to `personas/` + `config/feature-inventory.json` in T4.1); 7 unused scripts deleted (`analyze_hubs.py`, `backfill_venue_metadata.py`, `check_data_json_visual_arts.py`, `check_filter_smoke.py`, `check_variant.mjs`, `gitnexus_workflow.sh`, `overnight_loop.sh`).

## Carry-over work (the un-done bits)

These three items must be picked up by the user or in a follow-up run:

1. **T6.5 — historical `.long-run/` cleanup is on disk but commit was council-rejected.** The first commit (`3990262`) physically removed `.long-run/20260421-225013/`, `.long-run/20260422-203219/`, and `.long-run/20260425-175347/`. The rebuttal commit (`7c61087`) added `.gitignore` patterns under `.long-run/*/` to keep transient runner artifacts out of tracked history. The council's final objection was the commit subject of `7c61087` ("Cleanup historical .long-run session directories") didn't match its actual diff (only `.gitignore`). Both commits are on the branch; the deletions are real. **Recommended action**: keep both commits (the cleanup goal is achieved) and accept the misleading-subject finding as a known imperfection — or amend `7c61087`'s subject locally to `feat(task-6.5): ignore transient runner artifacts under .long-run` before merging to `main`.
2. **T6.6a / T6.6b — classical-scraper consolidation NOT done.** `src/scrapers/_static_json_scraper.py` does not exist. The five scrapers (`austin_symphony_scraper.py`, `early_music_austin_scraper.py`, `la_follia_scraper.py`, `austin_chamber_music_scraper.py`, `austin_opera_scraper.py`) still each duplicate the static-JSON-loading shape with hardcoded per-venue type. T6.6a crashed with `no-result-file (rc=1)` — the agent died before writing its result file. **Recommended action**: queue this on a future run with a stricter contract test for the result-file write, or build the base class manually. Estimated effort: 2–3h with full test migration.
3. **T7.3 — runner wrap-up.** Will fire automatically after this handoff commit lands — `validate=true`, `[no-council]`, no human action needed.

## Validation oracle

`/Users/HCornier/Documents/Personal/Culture-Calendar/.venv/bin/python -m pytest -q` (this worktree shares the parent's `.venv`) → **856 passed, 25 skipped, 1 warning** at handoff. The 25 skipped are `@pytest.mark.live` (require network) plus a small set of `@pytest.mark.integration`. T7.1 also verified `scripts/verify_calendar.py --offline` (22/22) and the live-site persona council (34 PASS / 0 FAIL).

## Feature inventory

`config/feature-inventory.json` = 73 entries at handoff (was 65 at run start). New entries appended this run:

| Phase | Source task | What was added |
|------:|-------------|----------------|
| 4 | T4.6 | `review-quality` + `repo-minimalism` reviewer surfaces (manual-run + pre-push touchpoints). |
| 5 | T5.2 | Web-font loading guard (link-tag presence in `docs/index.html`). |
| 5 | T5.3 | Type-scale CSS-variable presence (`--fs-display` resolves on `:root`). |
| 5 | T5.4 | Heading-level type-scale application (h1/h2 computed font-size). |
| 5 | T5.5 | Mobile `clamp()` viewport compression. |
| 5 | T5.6 | Persona-gate typography surface assertion (continuity-user). |

## Suggested next actions

1. **Optionally amend `7c61087` to silence the council finding** (one of):
   ```bash
   git checkout long-run/20260430-102637
   git rebase -i 15e9238   # interactive; pick reword for 7c61087
   #   reword 7c61087 → feat(task-6.5): ignore transient runner artifacts under .long-run
   ```
   This is **only** safe before the branch is shared. If you want to skip the reword, the diff is fine as-is — only the commit message is misleading.
2. **Merge to `main`.**
   ```bash
   git checkout main
   git merge --ff-only long-run/20260430-102637
   git push origin main
   ```
   `--ff-only` should succeed unless `main` advanced concurrently. Do **not** force-push.
3. **Dispatch the failing weekly workflow** (verify Phase 1 fixed it):
   ```bash
   gh workflow run update-calendar.yml
   gh workflow run pr-validation.yml --ref main
   gh run watch
   ```
4. **Smoke-test the live site after Pages deploy.** Open `https://hadrien-cornier.github.io/Culture-Calendar/` and verify:
   - Source Serif 4 + Inter loaded (`document.fonts` contains both).
   - Mobile viewport compresses headings via `clamp()`.
   - Ballet event detail panes mention choreographer/company/repertoire (re-rated post-T2.5).
   - Search dropdown still works; chevron SVGs still render.
5. **Schedule the carry-over scraper consolidation** for a future run (or do manually). Suggested cadence: a 2h focused run with `T6.6a` rewritten as two atomic sub-tasks (base class first, then per-venue migrations).
6. **Optional persona council pre-push.** T1.5, T5.6, T6.4 carried `[persona-gate]`. Run `git config core.hooksPath .githooks` once per clone, then push — the pre-push hook spins up `python -m http.server` on `docs/` and runs `scripts/require_persona_approval.py`. Pre-flight: `.venv/bin/python scripts/persona_critique.py --out docs/PERSONAS.md`.

## Cleanup commands (post-merge housekeeping)

After the branch is merged, the user may run:

```bash
# Delete the merged run branch locally:
git branch -d long-run/20260430-102637

# Remove the worktree (it lives outside main repo):
git worktree remove /Users/HCornier/Documents/Personal/Culture-Calendar-long-run-20260430-102637

# Trim the runner's per-task logs (already gitignored, but bulky on disk):
rm -rf /Users/HCornier/Documents/Personal/Culture-Calendar/.long-run/20260430-102637/task-*.log
rm -rf /Users/HCornier/Documents/Personal/Culture-Calendar/.long-run/20260430-102637/reviews

# Verify pytest still green from the main checkout:
cd /Users/HCornier/Documents/Personal/Culture-Calendar
.venv/bin/python -m pytest -q
```

The runner-state files at `.long-run/20260430-102637/{events.log, queue.tsv, scorecard.md}` are worth keeping for one cycle as audit evidence; everything else under that directory is regenerable runtime debris.

## Verification checklist

Before merging to `main`:

1. **Pytest** — `.venv/bin/python -m pytest -q` → expect `856 passed, 25 skipped`.
2. **Trimmed sizes** — `wc -l CLAUDE.md CHANGELOG.md` → expect `249` and `387`.
3. **No `.overnight/`** — `[ ! -d .overnight ] && echo OK`.
4. **No root logs** — `ls *.log 2>/dev/null | wc -l` → expect `0`.
5. **Personas in place** — `ls personas/code-review/ personas/live-site/` → 2 files in code-review, 6 in live-site.
6. **Feature inventory** — `grep -c '"id":' config/feature-inventory.json` → expect `73`.
7. **Classical refresh dry-run** — `.venv/bin/python scripts/refresh_classical_data.py --dry-run --venue austinSymphony` exits 0.
8. **`review_quality_check.py` works** — `.venv/bin/python scripts/review_quality_check.py --no-llm --staged` prints a prompt.

## Memory updates applied this run

None new during the run. Existing memory continued to apply:

- `feedback_git_author.md` — every commit on this branch is authored as `Hadrien-Cornier <hadrien.cornier@gmail.com>`.
- `feedback_overnight_queue_escapes.md` — queue.tsv `validate` column escapes were honored at scaffold time.
- `feedback_persona_gate_strict.md` — every council FAIL during this run was treated as blocking; the two BLOCKED tasks (6.5, 6.6a) were not waved through with "pre-existing on main" excuses.
